# Dependency build order (tiers from bom/tiers.json; within a tier, order is
# arbitrary). One name per line = .github/deps/<name>/ = nixpkgs attr.

# --- tier 0 ---
zlib
xz
bzip2
zstd
lzo
pcre2
openssl
boehmgc
brotli
nlohmann_json
editline
libev
libblake3
aws-c-common
lowdown
llhttp
libsodium
nghttp3

# tier 0 complete (18/21 built). Not built by design:
#   libiconv (both BOM copies) — links /usr/lib/libiconv.dylib (DESIGN.md §6.5)
#   c-ares, ncurses, publicsuffix-list — curl-slicing cuts (DESIGN.md §6.3)

# --- tier 1 ---
libunistring
sqlite
toml11
nghttp2
s2n-tls
aws-checksums
aws-c-compression
aws-c-sdkutils
aws-c-cal
ngtcp2
boost

libxml2

# tier 1: gettext, libedit, libssh2 not built — curl-slicing cuts
# (DESIGN.md §6.3). libxml2 is NOT a cut: libarchive needs it (xar).

# --- tier 2 ---
libidn2
aws-c-io
libgit2
libarchive

# tier 2: krb5, libxslt not built — curl-slicing cuts (DESIGN.md §6.3).
